services:
  app:
    build: .
    env_file:
      - .env

  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: hr_interview
    ports:
      - "5432:5432"